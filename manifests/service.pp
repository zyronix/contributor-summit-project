# This class starts EJBCA
#
# @summary Manage the service of EJBCA, should not be called directly
#
class ejbca::service {
  # Might not need this file, because JBoss will be the main service, but we will see.
}
